from .routes import bp

"""
This module imports and exposes the main Blueprint for API routes.
The Blueprint is defined in the 'routes' module and contains all the endpoint routes and their logic.
It can be registered with the Flask app to make the API endpoints accessible.
"""